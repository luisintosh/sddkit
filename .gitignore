# Runtime artifacts produced inside a consuming repo's .opencode/ — never commit these
usage.log
*.log

# Managed feature worktrees
.worktrees/

# OS / editor noise
.DS_Store
*.swp

# Node / Bun
node_modules/

# dist/ and manifest.txt are generated by `bun run build` and tracked so
# clients can install without transpiling. Never hand-edit them.
dist/bin/sddkit-state-darwin-*
