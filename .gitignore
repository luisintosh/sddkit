# Runtime artifacts produced inside a consuming repo's .opencode/ — never commit these
usage.log
*.log

# Managed feature worktrees
.worktrees/

# OS / editor noise
.DS_Store
*.swp

# Node (only relevant if the harness itself grows JS tooling)
node_modules/

# Generated per-harness install trees — built by build/*.mjs, published as
# release artifacts, never committed to main.
build/opencode/
build/cursor/
