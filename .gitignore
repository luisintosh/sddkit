# Runtime artifacts produced inside a consuming repo's .opencode/ — never commit these
usage.log
*.log

# Managed feature worktrees
.worktrees/

# OS / editor noise
.DS_Store
*.swp

# Node / Bun
node_modules/

# Generated install payload (built by `bun run build`; packaged on release)
dist/
manifest.txt
